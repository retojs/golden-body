{
  "name": "golden-body",
  "version": "1.0.0",
  "description": "Display of the Golden Body Model of Golden Body Spots",
  "main": "index.html",
  "scripts": {
    "start": "live-server src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "retojs",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
